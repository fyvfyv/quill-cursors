# TEMPORARY: 'plugin:@reedsy/recommended' is removed while the @reedsy
# GitHub Packages auth is unavailable locally. Restore the extends line
# (and the devDependency) to re-enable the style checks.
parser: '@typescript-eslint/parser'
parserOptions:
  ecmaVersion: 2022
  sourceType: module
env:
  node: true
  browser: true
